import { Html, useProgress } from '@react-three/drei'

const Loader = () => {
  const { progress } = useProgress()
  return (
    <div>
      This loader does not work yet
    </div>
  )
}

export default Loader